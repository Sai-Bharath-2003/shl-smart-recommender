server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # Remove the /api/ block entirely if you are using the full 
    # https://shl-smart-recommender.onrender.com URL in App.jsx.
}